import type { TRegisterRequest } from "../../types/auth.types.js";

const registerRequest = (payload: TRegisterRequest) => {
  // Implementation for handling registration request
};

export const AuthService = {
  registerRequest,
};
